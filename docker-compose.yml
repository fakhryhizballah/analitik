version: '3.8'

services:
  app:
    build: .
    restart: always
    container_name: analytics-app  # Memberi nama pada container aplikasi
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: npm start